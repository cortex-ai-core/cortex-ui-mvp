@tailwind base;
@tailwind components;
@tailwind utilities;

/* Optional: Smooth fonts + clean UI defaults */
html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background-color: white;
  color: black;
}

